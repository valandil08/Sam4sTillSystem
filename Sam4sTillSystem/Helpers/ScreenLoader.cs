using Sam4sTillSystem.Enum;
using Sam4sTillSystem.Model;
using System.Windows;
using System.Windows.Media;

namespace Sam4sTillSystem.Helpers
{
    public static class ScreenLoader
    {
        public static void LoadScreenData(MainWindow window, Screen screen)
        {
            switch (screen) 
            {

            }
        }

        public static void UnloadScreenData(MainWindow window, Screen screen)
        {
            switch (screen)
            {

            }
        }
    }
}
